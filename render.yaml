services:
  - type: web
    name: bakery-kitchen-manager
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: bakery-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
